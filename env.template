# ============================================
# Minuteserv Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# Command: cp env.template .env

# ============================================
# SUPABASE (REQUIRED)
# ============================================
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
# 1. Go to Project Settings > API
# 2. Copy "Project URL" → SUPABASE_URL
# 3. Copy "service_role" key (NOT anon key!) → SUPABASE_SERVICE_ROLE_KEY
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# JWT AUTHENTICATION (Optional but recommended)
# ============================================
# Generate a random secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# RAZORPAY PAYMENT (OPTIONAL - for payments)
# ============================================
# Get from: https://dashboard.razorpay.com/app/keys
# Only needed if you want payment functionality
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret
RAZORPAY_WEBHOOK_SECRET=your-razorpay-webhook-secret

# ============================================
# MSG91 SMS OTP (REQUIRED for OTP)
# ============================================
# Get from: https://control.msg91.com/app/m/l/settings/security/authkey
# 1. Go to MSG91 Dashboard > Settings > Authkey
# 2. Create new authkey (IP Security OFF for development)
# 3. Copy Auth Key → MSG91_AUTH_KEY
# 4. Template ID from MSG91 OTP Templates section
# 5. Sender ID from MSG91 (default: MSERV)
MSG91_AUTH_KEY=your-msg91-auth-key-here
MSG91_TEMPLATE_ID=6930040e475ae21f4f0c2ef9
MSG91_SENDER_ID=MSERV

# ============================================
# INTERAKT WHATSAPP OTP (OPTIONAL - Legacy)
# ============================================
# Keep these if you want to use Interakt WhatsApp OTP instead
# Get from: https://app.interakt.ai/settings/api-keys
INTERAKT_API_KEY=your-interakt-api-key-here
INTERAKT_BASE_URL=https://api.interakt.ai/v1/public

# ============================================
# SERVER CONFIGURATION (Optional)
# ============================================
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# ============================================
# ADMIN USER (Optional - for setup scripts)
# ============================================
ADMIN_EMAIL=admin@minuteserv.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=Admin User

# ============================================
# CREDENTIALS ENCRYPTION (REQUIRED for credentials management)
# ============================================
# Generate a random 32+ character string for encrypting stored credentials
# Command: openssl rand -hex 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Keep this key secure! If lost, encrypted credentials cannot be decrypted.
CREDENTIALS_ENCRYPTION_KEY=your-32-character-or-longer-encryption-key-here
